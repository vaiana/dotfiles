#!/bin/bash
pw_file="$HOME/.bw_master"
if [ ! -f "$pw_file" ]; then
    notify-send "bw-type-password" "Missing $pw_file"
    exit 1
fi
sleep 0.2
xdotool type --clearmodifiers -- "$(tr -d '\n' < "$pw_file")"
